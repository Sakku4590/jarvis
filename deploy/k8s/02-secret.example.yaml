# TEMPLATE ONLY. Do not commit real secrets. Render this with real values via a
# secrets manager (Sealed Secrets, External Secrets Operator, SOPS, or your
# cloud KMS), or create it imperatively:
#
#   kubectl -n jarvis create secret generic jarvis-secrets \
#     --from-literal=SECRET_KEY=$(openssl rand -hex 32) \
#     --from-literal=POSTGRES_PASSWORD=... \
#     --from-literal=CREDENTIAL_ENCRYPTION_KEY=$(python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())") \
#     --from-literal=OPENAI_API_KEY=... \
#     ...
apiVersion: v1
kind: Secret
metadata:
  name: jarvis-secrets
  namespace: jarvis
type: Opaque
stringData:
  SECRET_KEY: "CHANGE_ME_openssl_rand_hex_32"
  POSTGRES_PASSWORD: "CHANGE_ME"
  # Fernet key for encrypting stored OAuth tokens (Phase 8). 32-byte urlsafe b64.
  CREDENTIAL_ENCRYPTION_KEY: "CHANGE_ME_fernet_key"
  OPENAI_API_KEY: "CHANGE_ME"
  GOOGLE_CLIENT_ID: ""
  GOOGLE_CLIENT_SECRET: ""
  SPOTIFY_CLIENT_ID: ""
  SPOTIFY_CLIENT_SECRET: ""
  TWILIO_ACCOUNT_SID: ""
  TWILIO_AUTH_TOKEN: ""
  TWILIO_WHATSAPP_FROM: ""
  TWILIO_MESSAGING_SERVICE_SID: ""
